---
layout: "../../../layouts/Blog.astro"
title: My Second Post
date: 12/06/2022
description: It's another post by me.
tags: ["example", "blog"]
value: 128
---

## This is so cool!

Do variables work {frontmatter.value * 2}?

Does $\katex$ work?

Does math block work?

$$
e^{32} - 1 = 0
$$

does inline $e^{j\theta} = \cos\theta + j\sin\theta$ math work?

```python
# example python

def fib(x):
  if x in [0,1,2]:
    return 1
  return fib(x-1) + fib(x-2)
```

```rust
// example rust

fn fib(x: i32) -> i32:
  if x in [0,1,2]:
    1
  fib(x-1) + fib(x-2)
```
